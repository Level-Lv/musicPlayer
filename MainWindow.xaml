<Window x:Name="musicPlayerForm" x:Class="musicPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:musicPlayer"
        mc:Ignorable="d"
        Title="音乐播放器" Height="600" Width="1000">
    <Window.Resources>
        <Storyboard x:Key="SpinningAnimation" x:Name="SpinningStoryboard">
            <DoubleAnimation 
            Storyboard.TargetName="RotationTransform" 
            Storyboard.TargetProperty="Angle" 
            From="0" To="360" 
            Duration="0:0:5"         RepeatBehavior="Forever" />
        </Storyboard>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="18.5*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300*"/>
            <ColumnDefinition Width="700*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Margin="5">
            <Button x:Name="btnSelectFolder" 
                HorizontalAlignment="Left"
                Padding="8,4"
                Click="btnSelectFolder_Click">
                📁 选择文件夹
            </Button>
            <Button x:Name="btnShuffle"
                HorizontalAlignment="Right"
                Padding="8,4">
                🔀 随机播放
            </Button>
        </Grid>

        <ListView x:Name="musicfilesListView"
          d:ItemsSource="{d:SampleData ItemCount=5}"
          Grid.Column="0"
          Grid.Row="1"
          Margin="1"
          MouseDoubleClick="musicfilesListView_MouseDoubleClick">

            <ListView.Resources>
                <Style TargetType="GridViewColumnHeader">
                    <Setter Property="Background" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="0"/>

                    <Setter Property="Foreground" Value="LightGray"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="GridViewColumnHeader">
                                <Grid>
                                    <Border x:Name="HeaderBorder" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">

                                        <ContentPresenter x:Name="HeaderContent" 
                                              Margin="0" 
                                              RecognizesAccessKey="True" 
                                              Content="{TemplateBinding Content}" 
                                              ContentTemplate="{TemplateBinding ContentTemplate}" 
                                              ContentStringFormat="{TemplateBinding ContentStringFormat}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="DarkGray"/>
                            <Setter Property="Foreground" Value="Silver"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Resources>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="歌曲"
                            Width="160"
                            DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="时长"
                            Width="100"
                            DisplayMemberBinding="{Binding Duration, StringFormat={}{0:mm\\:ss}}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <Grid Grid.Row="1" Grid.Column="1" Margin="1">

            <Viewbox Width="350" Height="350" HorizontalAlignment="Center" VerticalAlignment="Center">

                <Grid x:Name="VisualizerGrid" Width="100" Height="100">
                    <Grid.RenderTransform>
                        <RotateTransform x:Name="RotationTransform" CenterX="50" CenterY="50"/>
                    </Grid.RenderTransform>

                    <Ellipse Fill="#303030" Stroke="Silver" StrokeThickness="1" Width="100" Height="100"/>
                    <Ellipse Fill="#101010" Stroke="DarkGray" StrokeThickness="0.5" Width="70" Height="70"/>
                    <Ellipse Fill="#505050" Stroke="Silver" StrokeThickness="1" Width="10" Height="10"/>

                    <Rectangle Width="5" Height="100" Fill="#202020"/>
                    <Rectangle Width="100" Height="5" Fill="#202020"/>
                </Grid>

            </Viewbox>

            <MediaElement x:Name="mediaPlayer"
                      LoadedBehavior="Manual"
                      Visibility="Collapsed"/>
        </Grid>

    </Grid>
</Window>
